server:
  port: 8081
spring:
  application:
    name: tgid-bruno
  datasource:
    url: jdbc:h2:mem:teste
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
